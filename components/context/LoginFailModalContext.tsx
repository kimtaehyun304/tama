/*
"use client";
import { createContext } from "react";

type ModalType = {
  isOpenLoginFailModal: boolean;
  setIsOpenLoginFailModal: (isOpen: boolean) => void;
};

const LoginFailModalContext = createContext<ModalType | null>(null);

export default LoginFailModalContext;
*/